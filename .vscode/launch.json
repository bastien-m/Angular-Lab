{
    "version": "1.0.0",
    "configurations": [
        {
            "name": "attach to localhost",
            "type": "chrome",
            "request": "attach",
            "sourceMaps": true,
            "port": 9222,
            "webRoot": "${workspaceRoot}"
        }
    ]
}